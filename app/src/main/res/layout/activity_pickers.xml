<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center">

    <!-- TextView for displaying selected date -->
    <TextView
        android:id="@+id/tv_selected_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/selected_date_none"
        android:textSize="16sp"
        android:layout_marginBottom="16dp" />

    <!-- Button for opening Date Picker -->
    <Button
        android:id="@+id/btn_date_picker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/pick_a_date"
        android:layout_marginBottom="16dp" />

    <!-- TextView for displaying selected time -->
    <TextView
        android:id="@+id/tv_selected_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/selected_time_none"
        android:textSize="16sp"
        android:layout_marginBottom="16dp" />

    <!-- Button for opening Time Picker -->
    <Button
        android:id="@+id/btn_time_picker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/pick_a_time" />
</LinearLayout>
